# Handoff — 14 September 2026, 09:20

**Written at the final commit of the sign-off.** If commits have landed after it, this document is
history and not truth — the session-start hook counts them and names them; read those first.

---

## 1. The state, and what went wrong

**Nothing is broken, nothing is unpushed, and the worst mistake of the week was mine.**

**956 tests green, 0 failing, 201.76 seconds across 51 files — measured this morning in the browser,
not quoted from an earlier run.** No game code, `data/`, `content/` or `DESIGN.md` was touched.

**THE MISTAKE.** The wiki prints, beside every rule, whether a later ruling replaced it. The check
for that searched the note about each rule for the word *"superseded"* — and **most of those notes
open with the words "Not superseded."** It read the denial as a confirmation and stamped **39 live
rules** with *"SUPERSEDED — do not state this as a rule"*. The writer Aaron had hired was drafting
against that, spotted it, and said so on six separate pages of their questions file before anyone
here did. Fixed, tested against eight known cases, **the true number is six.** Recorded as **rule 12**
and in **D211**.

**Round 3 is closed** — Aaron approved `close-round-3` on the board at 2026-09-12T02:41:50Z.
**Round 4, the economy, is the live round and has not been started.**

---

## 2. Read these first, in this order

| | Why |
|---|---|
| `DECISIONS.md` **D210 and D211** | The wiki, and the three corrections D211 makes to D210 |
| `docs/deferred.md` **17–29** | Twenty confirmed defects not fixed. **None makes the wiki state something false** — that class was fixed |
| `docs/PROGRAMMER-RULES.md` **12 and 13** | Both earned this session, and 13 will bite you within the hour if you ignore it |
| `docs/wiki/README.md` | How to re-run the generator, and what it will and will not overwrite |
| `docs/design/IDEATION-PLAN.md` | Round 4 is live |

---

## 3. What exists now

**`docs/wiki/` — 81 generated pages.** Eleven master subjects, 28 topic pages, 26 movements, 10
political positions, 3 ruling indexes, a movement register and a front page. **It has no prose in
it.** Every page carries its headings, its figures, its candidate links and a Sources table of the
rulings that feed it. Open the **repository root** as an Obsidian vault, not `docs/wiki/` — the
decisions log and the built-game document live at the root and the graph should reach them.

**`docs/wiki_new/` — the hired writer's 76 written articles.** Committed exactly as delivered, then
reviewed. **Not merged. That is Aaron's decision and it is the one card on the board.**

**The systems map** — https://claude.ai/code/artifact/9fe7b1c4-405b-427f-8406-9c1f10baea90 —
rebuilt with `python build/build_systems_map.py` and republished to that same address.

**Five files in `build/`** that a design session would normally not be allowed to touch:
`build_wiki.py`, `build_systems_map.py`, `systems_map_template.html`, and the `wiki_*.json` inputs.
**The exception is now written into the designer brief** with its date and its limit, rather than
left as a silent violation.

---

## 4. The rules you must not get wrong

**A design session does not touch code or data.** Not `js/`, `tests/`, `css/`, `index.html`,
`dev.html`, `server.py`; not `data/` or `content/`; not `DESIGN.md`; not `docs/spec/` without
permission. **The one exception is the wiki's own generator in `build/`**, named above. You MAY read
all of it — do it.

**The Control Board is edited with the file editor, never generated by a shell script with layers of
quoting.** It has been damaged that way twice. Validate before publishing: the HTML parses, and the
`BOARD` object evaluates under `node`. Both checks are in this session's transcript and both were run.

**Rule 13, and I broke it three times today:** do not build a Python string in a shell heredoc when
it contains backslash escapes. `\b` became a literal backspace in a regex that then silently matched
nothing; `\n` became a real newline that split a JavaScript string and broke the board. **Use the
file editor for any patch containing an escape.**

---

## 5. What the sign-off review found

Sixty-one agents across four dimensions — the generator, its data, the map, and the documents — each
told to attack, and every finding then given to a second agent told to refute it. **57 raised, 12
refuted, 45 confirmed, 23 of them serious. Nine fixed, twenty deferred.**

The three besides the supersession bug that mattered:

- **A writer quoting a generated-block marker at the start of a line** would have had their prose
  silently eaten on the next run — the block pattern pairs any opening marker with the *next*
  closing one. **Nothing has been lost**; no page in either wiki triggers it. The generator now
  refuses to write a page whose markers are ambiguous and names it in the report.
- **Round 1's first eleven rulings are rows of one table**, and reading a ruling's body forward from
  a row swallowed every row beneath, putting **27 false citations on five movement pages**.
- **The derived "At a glance" block sat outside the regenerating part of a page** and froze at
  whatever was true the day the page was made. Ten pages were already stale.

---

## 6. Your first job

**Ask him the card on the board: keep the writer's 76 articles, or start again.** My recommendation
is recorded there and it is *keep* — the expensive part of this job is verifying every claim against
the rulings, and that cost is the same either way, so one road keeps 5,400 words already in his voice
and the other throws them away.

**If he says keep**, the order is: repair the writer's copy with
`python build/build_wiki.py --out docs/wiki_new` — which clears **94 stale SUPERSEDED labels** and
touches no prose — then verify every claim on every page against the rulings, then rewrite the four
pages built on a misreading rather than a slip: **Grievance**, **The political board**, **War** and
**Release valves**.

**Read `docs/deferred.md` 17 first** before running that repair: the "At a glance" block moved inside
a generated block on the same day the writer's copy was made, so on their pages the generator will
append it at the foot rather than put it back in place. One ugly block per page, or merge first.

**He has parked the restructure** — folding the eleven subjects into five nested sections. Do not
start it unasked.

---

## 7. What is his, and what is not

**His:** the one card — the writer's articles. And the restructure, which he has parked.

**Not his:** anything in `docs/deferred.md`, the generator, or the systems map. **Do not ask him to
re-decide anything already ruled** — 136 rulings are recorded across three closed rounds.

---

## 8. Known but unverified

- **The game has not been opened since 5 September.** Only the test page was run today.
- **There is still no committed list of nations.** The only nation-shaped file is an autosave from
  turn 4 of one playthrough, and it is gitignored. Nation pages are keyed on display names.
- **Obsidian "Bases" is enabled in his config and nobody has established what it does** — its
  documentation page returned nothing when asked. Ten minutes in the app would settle whether it
  already does part of the restructure.
- **Fourteen wiki pages will ship with an empty story section**, because round 1's thirteen-step
  break-up narrative is the only sustained fiction in the project.
- The repository is **public and that is correct** — his decision of 7 September. **Do not raise it.**

---

## 9. Before you stop, every time

Run `/signoff`, and **write the handoff last**. If you commit anything after it, rewrite it —
step 7 of the sign-off proves it with a commit count that must print `0`.
