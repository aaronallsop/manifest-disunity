# Handoffs

One file per session end, named `YYYY-MM-DD_HHMM_<project>_handoff.md` so the folder sorts by
date and the last file is always the current one. `/resume` reads the newest automatically;
`/signoff` and `/handoff` write the next one. Older files are history — do not edit them.
