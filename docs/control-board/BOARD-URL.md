# Control Board

Published address (stable — republish to this URL, never create a new one):

    https://claude.ai/code/artifact/aae9bdbf-3309-4df1-be0f-471abb77fa1d

Source: `board.html` in this folder. Edit only the `BOARD` object in its script.
Saved records: `decisions/<id>` and `settings/permissions` in the artifact database.

First published 2026-09-04. The page is private to Aaron unless shared from its own share menu.

---

# Sector Wiring

A second published page, built 14 September 2026 at Aaron's request during round 4:

    https://claude.ai/code/artifact/9c022522-a1be-4219-8e5d-ff4d2b61ab62

Source: `docs/design/resource-map.html`. The six sectors on a ring; click one, then another, to draw
an arrow and say what it does. Saved records: `links/<id>` in that artifact's database, each
`{from, to, kind, note, source}` where `source` is `ruled` (carried from a ruling) or `aaron` (drawn
by him). **Read it with `read_db` at the start of a session** — what he draws there is design input,
the same as a note on the Control Board.

Seeded 14 September with the six relationships established by the economy spec and round 4.
